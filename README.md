# DOTFILES

## Ubuntu

```bash
sudo apt update
sudo apt install zsh stow fzf -y
zsh install-zsh-plugins.zsh && source ~/.zshrc
```

## MacOS

```bash
brew install zsh stow fzf
zsh install-zsh-plugins.zsh && source ~/.zshrc
```
